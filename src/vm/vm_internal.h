#ifndef SLATE_VM_INTERNAL_H
#define SLATE_VM_INTERNAL_H

#include "vm.h"

// Internal VM shared declarations
// Most functions are already properly declared in the public vm.h header
// This file is for any private helper functions shared between VM modules

// Forward declarations for internal helper functions
// (Currently none needed - all functions are properly declared in vm.h)

#endif // SLATE_VM_INTERNAL_H