#include "number.h"
#include "builtins.h"

// Number has no instance methods since there are no methods that apply identically to ALL numeric types
// Individual numeric types (Int, Float) implement their own versions of abs(), sign(), etc.